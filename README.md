# Local Kubernetes Labs

Two ways to run a local Kubernetes cluster, pick whichever fits:

- [`kind/`](kind/README.md) — single-command, Docker-based, fast to spin up and tear down.
- [`kubeadm/`](kubeadm/README.md) — 3-node VirtualBox VM cluster via `kubeadm`, closer to a real bare-metal setup.
